function setup() {
  createCanvas(400, 400);
}

function draw() {
  colorMode(HSB);
  background(255);


  for (var x = 1; x < width - 1; x = x + 100) {
     fill(random(255), 100, 255); //why isn't the colors filling?
    line(x, height / 2, mouseX, mouseY);
  }
  }
